import { render, screen } from '@testing-library/react';
import Signup from './component/Signup';

// test('Sign up mock test', () => {
//   render(<Signup />);
//   const linkElement = screen.getByText(/Sign Up/i);
//   expect(linkElement).toBeInTheDocument();
// });
